import { useAppStore, AppStore, HistoryItem } from '@/store/AppStore';
import { useAnimationStore, AnimationStore } from '@/store/AnimationStore';

export { useAppStore, useAnimationStore };
export type { AppStore, AnimationStore, HistoryItem };
